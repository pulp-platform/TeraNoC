diff --git a/hardware/deps/floo_noc/hw/floo_meta_buffer.sv b/hardware/deps/floo_noc/hw/floo_meta_buffer.sv
index 1293f46..ca48bc8 100644
--- a/hardware/deps/floo_noc/hw/floo_meta_buffer.sv
+++ b/hardware/deps/floo_noc/hw/floo_meta_buffer.sv
@@ -165,7 +165,9 @@ module floo_meta_buffer #(
       .oup_req_i        ( axi_rsp_i.b_valid           ),
       .oup_data_o       ( no_atop_b_buf               ),
       .oup_data_valid_o ( b_oup_data_valid            ),
-      .oup_gnt_o        ( b_oup_gnt                   )
+      .oup_gnt_o        ( b_oup_gnt                   ),
+      .full_o           (                             ),
+      .empty_o          (                             )
     );
 
     id_queue #(
@@ -190,7 +192,9 @@ module floo_meta_buffer #(
       .oup_req_i        ( axi_rsp_i.r_valid           ),
       .oup_data_o       ( no_atop_r_buf               ),
       .oup_data_valid_o ( r_oup_data_valid            ),
-      .oup_gnt_o        ( r_oup_gnt                   )
+      .oup_gnt_o        ( r_oup_gnt                   ),
+      .full_o           (                             ),
+      .empty_o          (                             )
     );
 
     assign ar_no_atop_buf_full = !ar_no_atop_buf_not_full;
diff --git a/hardware/deps/floo_noc/hw/floo_nw_chimney.sv b/hardware/deps/floo_noc/hw/floo_nw_chimney.sv
index 166b5cb..6617c9f 100644
--- a/hardware/deps/floo_noc/hw/floo_nw_chimney.sv
+++ b/hardware/deps/floo_noc/hw/floo_nw_chimney.sv
@@ -1176,9 +1176,9 @@ module floo_nw_chimney
     // Reduction --> Multicast the B response to all members
     floo_narrow_b.hdr.collective_op  = floo_pkg::collect_op_e'('0);
     if(en_narrow_collective(CollectOpCfg)) begin: gen_nar_b_coll
-      if(is_multicast_op(narrow_aw_buf_hdr_out.hdr.collective_op)) begin: gen_nar_red_rsp
+      if(is_multicast_op(floo_pkg::collect_op_e'(narrow_aw_buf_hdr_out.hdr.collective_op))) begin: gen_nar_red_rsp
         floo_narrow_b.hdr.collective_op = CollectB;
-      end else if(is_reduction_op(narrow_aw_buf_hdr_out.hdr.collective_op)) begin: gen_nar_mcast
+      end else if(is_reduction_op(floo_pkg::collect_op_e'(narrow_aw_buf_hdr_out.hdr.collective_op))) begin: gen_nar_mcast
         floo_narrow_b.hdr.collective_op = Multicast;
       end
     end
@@ -1267,9 +1267,9 @@ module floo_nw_chimney
     // Reduction --> Multicast the B response to all members
     floo_wide_b.hdr.collective_op  = Unicast;
     if(en_wide_collective(CollectOpCfg)) begin: gen_wide_b_coll
-      if(is_multicast_op(wide_aw_buf_hdr_out.hdr.collective_op)) begin: gen_wide_red_rsp
+      if(is_multicast_op(floo_pkg::collect_op_e'(wide_aw_buf_hdr_out.hdr.collective_op))) begin: gen_wide_red_rsp
         floo_wide_b.hdr.collective_op = CollectB;
-      end else if(is_reduction_op(wide_aw_buf_hdr_out.hdr.collective_op)) begin: gen_wide_mcast
+      end else if(is_reduction_op(floo_pkg::collect_op_e'(wide_aw_buf_hdr_out.hdr.collective_op))) begin: gen_wide_mcast
         floo_wide_b.hdr.collective_op = Multicast;
       end
     end
diff --git a/hardware/deps/floo_noc/hw/floo_output_arbiter.sv b/hardware/deps/floo_noc/hw/floo_output_arbiter.sv
index 7926162..9603c4f 100644
--- a/hardware/deps/floo_noc/hw/floo_output_arbiter.sv
+++ b/hardware/deps/floo_noc/hw/floo_output_arbiter.sv
@@ -56,7 +56,7 @@ module floo_output_arbiter import floo_pkg::*;
     reduce_mask = '0;
     if (EnParallelReduction) begin
       for (int i = 0; i < NumParallelRedRoutes; i++) begin
-        reduce_mask[i] = (is_parallel_reduction_op(data_i[i].hdr.collective_op));
+        reduce_mask[i] = (is_parallel_reduction_op(collect_op_e'(data_i[i].hdr.collective_op)));
       end
     end
   end
diff --git a/hardware/deps/floo_noc/hw/floo_nw_router.sv b/hardware/deps/floo_noc/hw/floo_nw_router.sv
index a6e63b5..c93bc4b 100644
--- a/hardware/deps/floo_noc/hw/floo_nw_router.sv
+++ b/hardware/deps/floo_noc/hw/floo_nw_router.sv
@@ -167,4 +167,6 @@ module floo_nw_router
       assign floo_wide_o[i].credit = wide_credit_out[i];
       assign wide_credit_in[i] = floo_wide_i[i].credit;
     end
+  end else begin : gen_no_credit_connections
+    assign wide_credit_in = '0;
   end
diff --git a/hardware/deps/floo_noc/hw/floo_router.sv b/hardware/deps/floo_noc/hw/floo_router.sv
index e6c6a50..b3b7864 100644
--- a/hardware/deps/floo_noc/hw/floo_router.sv
+++ b/hardware/deps/floo_noc/hw/floo_router.sv
@@ -362,6 +362,7 @@ module floo_router
       end
       if (!EnMultiCast) begin : gen_unicast
         assign cross_ready[in][v] = |(masked_ready_transposed[in][v] & route_mask[in][v]);
+        assign past_handshakes_q[in][v] = '0;
       end else begin : gen_multicast
         // In the case of multicast transactions, each destination can assert the ready signal
         // independently and potentially at different clock cycles. This logic ensures that
@@ -384,4 +385,5 @@ module floo_router
         // Send ready upstream only when all expected downstream handhsalkes have been received
         assign cross_ready[in][v] = &(all_handshakes[in][v] | ~expected_handshakes[in][v]);
+        `FF(past_handshakes_q[in][v], past_handshakes_d[in][v], '0)
       end
     end
@@ -390,5 +392,2 @@ module floo_router
-  // TODO (lleone): Move the folloiwng FF inside the multicast
-  `FF(past_handshakes_q, past_handshakes_d, '0)
-
   // We merge the data from the reduction module as an additional input of our output arbiter.
   logic [NumOutput-1:0][NumVirtChannels-1:0][LocalNumInputs-1:0] merged_valid, merged_ready;
